# --- Phase 1: selection logic (offline-testable) ---
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0
pytest>=8.0

# --- Phase 2: x402 proxy seller + payer (mock crypto) ---
fastapi>=0.110         # seller proxy
uvicorn>=0.29          # run the proxy
httpx>=0.27            # FastAPI TestClient

# --- Phase A: real on-chain payment (Base Sepolia testnet) ---
# Verified API: x402 v2.14.0. The [evm] extra pulls eth-account/web3 for EIP-3009 signing.
x402[evm]>=2.14
# Optional, only for the Coinbase CDP hosted facilitator (public facilitator needs none):
# cdp-sdk

# --- Phase 5: frontier backends (final test only) ---
# anthropic>=0.40
# openai>=1.40
